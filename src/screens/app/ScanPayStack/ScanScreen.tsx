import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const ScanScreen = () => {
  return (
    <SafeAreaView className="bg-indigo-500 flex-1 items-center justify-center">
      <Text className="text-white">ScanScreen</Text>
    </SafeAreaView>
  );
};
export default ScanScreen;
